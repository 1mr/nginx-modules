name: Build Nginx Modules

on:
  workflow_dispatch:
    inputs:
      nginx_version:
        description: Nginx version
        default: '1.30.2'
        required: true
  push:
    branches: ['master', 'main']
    paths:
      - modules.sh
      - ngx_brotli/Dockerfile
      - ngx_geoip2/Dockerfile
      - ngx_vts/Dockerfile
      - .github/workflows/modules.yml

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        module: [ngx_brotli, ngx_geoip2, ngx_vts]
        platform: [linux/amd64, linux/arm64]

    steps:
      - uses: actions/checkout@v6.0.2

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v4.0.0

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4.1.0

      - name: Set arch
        run: |
          ARCH=$(echo '${{ matrix.platform }}' | tr '/' '-')
          echo "ARCH=${ARCH}" >> "$GITHUB_ENV"
          echo "TARGETARCH=${ARCH#linux-}" >> "$GITHUB_ENV"

      - name: Build ${{ matrix.module }}
        run: |
          docker buildx build \
            --platform "${{ matrix.platform }}" \
            --build-arg NGINX_VERSION=${{ env.NGINX_VERSION }} \
            --build-arg TARGETARCH=${{ env.TARGETARCH }} \
            --output type=local,dest=./output \
            -f ${{ matrix.module }}/Dockerfile \
            .

      - name: Upload artifacts
        uses: actions/upload-artifact@v7.0.1
        with:
          name: ${{ matrix.module }}-${{ env.NGINX_VERSION }}-${{ env.ARCH }}
          path: ./output/

  release:
    runs-on: ubuntu-latest
    needs: [build]

    steps:
      - name: Download artifacts
        uses: actions/download-artifact@v8.0.1
        with:
          path: ./artifacts

      - name: Package artifacts
        run: |
          for dir in ./artifacts/*/; do
            name=$(basename "$dir")
            cd "$(pwd)/${dir}${{ env.NGINX_VERSION }}"
            tar -czf "${root}/${name}.tar.gz" .
          done

      - name: Create release
        uses: softprops/action-gh-release@v3.0.0
        with:
          tag_name: nginx-${{ env.NGINX_VERSION }}
          name: Nginx ${{ env.NGINX_VERSION }} Modules
          make_latest: true
          files: '*.tar.gz'
